import { FunctionComponent } from 'react';
import Heading from './Heading';
import styled from 'styled-components';

const Chart: FunctionComponent = () => {
  return <Heading as="h2">My Skills</Heading>;
};

export default Chart;
